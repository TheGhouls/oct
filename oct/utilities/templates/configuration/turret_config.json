{
    "name": "{{ name }}",
    "cannons": {{ cannons }},
    "rampup": {{ rampup }},
    "script": "{{ script }}",
    "hq_address": "{{ hq_address }}",
    "hq_publisher": {{ hq_publisher }},
    "hq_rc": {{ hq_rc }}
}
